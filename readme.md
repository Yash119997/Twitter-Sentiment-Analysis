Pre-requirements:
You'll need one of this- docker-compose or docker desktop

Steps to run it(How to):
1. Open terminal
2. Change directory into the unzipped folder where docker-compose.yml file is present
3. Run following command
> docker compose up -d
4. Wait for 5-10 mins(this may vary as per your internet speed)